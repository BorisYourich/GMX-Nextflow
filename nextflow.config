manifest {
    mainScript = '~/GMX-Nextflow/precy2.nf'
}

docker {
    runOptions= "-v $HOME:$HOME"
    enabled = true
    temp = 'auto'
    mountFlags = "Z"
}

process {
    container = "ljocha/gromacs:2021-2"
}
